# Compiled executables
server
client
test*

# Object files and debug symbols (C build artifacts)
*.o
*.dSYM/

# Live database files (contain user/book/allocation data — do not commit)
# Keep the directory itself tracked via a .gitkeep if needed
db_files/*.bin

# macOS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor / IDE artifacts
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
.dockerignore
